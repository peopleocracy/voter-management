jar {
    manifest {
        attributes 'Implementation-Title': 'Voter Management DB', 'Implementation-Version': version
    }
}

dependencies {
    compile group: 'org.peopleocracy', name: 'vote-db', version: version
}